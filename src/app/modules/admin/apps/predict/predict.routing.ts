import { Route } from '@angular/router';
import { PredictlistComponent } from './predictlist/predictlist.component';

export const predictlistRoutes: Route[] = [

    {
        path     : 'predictlist',
        component: PredictlistComponent,
    },

]
